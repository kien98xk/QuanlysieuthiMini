@model IEnumerable<QuanLySieuthimini1.Models.Nhomhang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Danh sách nhóm hàng</h2>

<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_NH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_NH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Ma_NH }) |
            @Html.ActionLink("Details", "Details", new { id=item.Ma_NH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Ma_NH })
        </td>
    </tr>
}

</table>
