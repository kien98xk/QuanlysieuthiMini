@model IEnumerable<QuanLySieuthimini1.Models.Nhacungcap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ma_NCC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ten_NCC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Diachi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SĐT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Ma_NCC)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Ten_NCC)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Diachi)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SĐT)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Email)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Ma_NCC }) |
        @Html.ActionLink("Details", "Details", new { id = item.Ma_NCC }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Ma_NCC })
    </td>
</tr>
}

</table>
